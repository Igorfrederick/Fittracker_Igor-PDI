/**
 * Índice de Models
 * 
 * Centraliza a exportação de todos os models da aplicação.
 * Facilita importações em outros arquivos.
 * 
 * Uso:
 * const { Treino } = require('./models');
 */

const Treino = require('./Treino');

module.exports = {
  Treino
};
