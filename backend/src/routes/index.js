/**
 * Índice de Rotas
 * 
 * Centraliza a exportação de todas as rotas da aplicação.
 * Facilita a organização e manutenção do código.
 */

const treinosRoutes = require('./treinos');

module.exports = {
  treinosRoutes
};
