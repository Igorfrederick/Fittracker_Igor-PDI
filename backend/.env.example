# ===========================================
# FitTracker - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha com seus valores

# Porta do servidor (padrão: 3000)
PORT=3000

# String de conexão do MongoDB Atlas
# Substitua <usuario>, <senha> e <cluster> pelos seus dados
# Formato: mongodb+srv://<usuario>:<senha>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://seu_usuario:sua_senha@seu_cluster.mongodb.net/fittracker?retryWrites=true&w=majority

# Ambiente (development, production, test)
NODE_ENV=development
